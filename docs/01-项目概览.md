# 01-项目概览

# 项目概览
## 项目定位
Vue 3 + TypeScript 入门实战项目，帮助初学者理解前后端如何通信。包含一个 Vue 3 前端和两个功能完全等价的后端（TypeScript / Python），在对比中学习。
## 功能一览

<lark-table rows="6" cols="3" header-row="true" column-widths="244,244,244">

  <lark-tr>
    <lark-td>
      功能
    </lark-td>
    <lark-td>
      说明
    </lark-td>
    <lark-td>
      涉及知识点
    </lark-td>
  </lark-tr>
  <lark-tr>
    <lark-td>
      计数器 + 双向绑定
    </lark-td>
    <lark-td>
      输入名字、加减计数
    </lark-td>
    <lark-td>
      ref、computed、v-model、v-if、v-for
    </lark-td>
  </lark-tr>
  <lark-tr>
    <lark-td>
      本地待办事项
    </lark-td>
    <lark-td>
      纯前端，数据存在内存
    </lark-td>
    <lark-td>
      组件化、响应式数组操作
    </lark-td>
  </lark-tr>
  <lark-tr>
    <lark-td>
      API 待办事项
    </lark-td>
    <lark-td>
      通过 fetch() 调用后端 CRUD
    </lark-td>
    <lark-td>
      REST API、JSON、onMounted
    </lark-td>
  </lark-tr>
  <lark-tr>
    <lark-td>
      后端切换
    </lark-td>
    <lark-td>
      下拉框一键切换 TS / Python 后端
    </lark-td>
    <lark-td>
      同一套 API，不同实现
    </lark-td>
  </lark-tr>
  <lark-tr>
    <lark-td>
      实时监控面板
    </lark-td>
    <lark-td>
      WebSocket 推送 CPU/内存/请求日志
    </lark-td>
    <lark-td>
      WebSocket、断线重连、实时数据流
    </lark-td>
  </lark-tr>
</lark-table>

## 技术栈

<lark-table rows="5" cols="3" header-row="true" column-widths="244,244,244">

  <lark-tr>
    <lark-td>
      层级
    </lark-td>
    <lark-td>
      技术
    </lark-td>
    <lark-td>
      版本
    </lark-td>
  </lark-tr>
  <lark-tr>
    <lark-td>
      前端
    </lark-td>
    <lark-td>
      Vue 3 + TypeScript + Vite
    </lark-td>
    <lark-td>
      Vue 3.5 / TS 5.7 / Vite 6
    </lark-td>
  </lark-tr>
  <lark-tr>
    <lark-td>
      TS 后端
    </lark-td>
    <lark-td>
      Express + better-sqlite3 + ws
    </lark-td>
    <lark-td>
      Express 4
    </lark-td>
  </lark-tr>
  <lark-tr>
    <lark-td>
      Python 后端
    </lark-td>
    <lark-td>
      Flask + flask-sock + psutil
    </lark-td>
    <lark-td>
      Flask 3.1
    </lark-td>
  </lark-tr>
  <lark-tr>
    <lark-td>
      数据库
    </lark-td>
    <lark-td>
      SQLite（零配置，文件型）
    </lark-td>
    <lark-td>
      —
    </lark-td>
  </lark-tr>
</lark-table>

## 项目结构
```plaintext
vue3-starter/
├── index.html                     # 入口 HTML
├── package.json                   # 前端依赖
├── vite.config.ts                 # Vite 配置
├── tsconfig.json                  # TypeScript 配置
├── frontend/                      # 前端源码
│   ├── main.ts                    # 应用入口，挂载 Vue 实例
│   ├── App.vue                    # 根组件（计数器 + 概念列表）
│   └── components/
│       ├── TodoList.vue           # 待办事项（纯前端版）
│       ├── TodoListApi.vue        # 待办事项（API 版）
│       └── Monitor.vue            # 系统监控面板（WebSocket）
├── backend-ts/                    # TypeScript 后端
│   └── src/
│       ├── index.ts               # Express 路由 + WebSocket 服务
│       └── db.ts                  # SQLite 数据库 CRUD 操作
└── backend-py/                    # Python 后端
    ├── app.py                     # Flask 路由 + WebSocket 服务
    └── db.py                      # SQLite 数据库 CRUD 操作

```

## 仓库信息
- GitHub: https://github.com/adam0wanna0top/vue3-starter
- 主分支: master

