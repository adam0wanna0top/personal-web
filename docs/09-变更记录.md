# 09-变更记录

# 变更记录
## v1.0.0 — 2026-04-11
### 初始版本
- Vue 3 + TypeScript + Vite 6 前端框架搭建
- Express + better-sqlite3 TypeScript 后端
- Flask + SQLite Python 后端
- REST API 实现完整 Todo CRUD
- WebSocket 实时监控面板
- 前端后端切换功能（TS / Python）
- 完整的教学文档（GUIDE.md、README.md、DEVELOPMENT.md）
- Claude Code 知识库集成（飞书 Wiki）
## v1.1.0 — 2026-04-12
### CI 基础设施与测试框架
- 新增 GitHub Actions CI 流水线（`.github/workflows/ci.yml`）：前端/TS后端/Python后端 三个 Job 并行
- 新增 Release 工作流（`.github/workflows/release.yml`）：打 tag 自动构建并创建 GitHub Release
- 前端集成 Vitest + Vue Test Utils + happy-dom 测试框架
- TS 后端集成 Vitest 测试框架，新增 `db.test.ts` 数据库单元测试
- Python 后端新增 `test_db.py` pytest 单元测试，db.py 函数增加 `db_path` 参数支持测试隔离
- 简化分支模型：移除 develop/release 分支，功能分支直接通过 PR 合入 master
- DEVELOPMENT.md 更新：新增测试框架章节，CI 改为三 Job 并行，分支策略简化为三种
**Bot 权限测试成功**
## v1.1.1 — 2026-04-12
### Wiki Sync 工作流优化
- 修改 Wiki Sync 工作流使用 Claude Code CLI 替代 GitHub Action
- 添加 ANTHROPIC_API_KEY 环境变量配置
- 通过 npm install -g 安装 CLI
- 使用 claude -p 命令直接调用，简化配置
**提交**: 4a89cfe**文件**: .github/workflows/wiki-sync.yml

